import {ScheduleDate} from '../schedule-models/ScheduleDate';
import {ClassTime} from '../schedule-models/СlassTime';

export class ScheduleBlocksGenerator {
  constructor(
    public classTime: ClassTime[],
  ) {
  }
}
