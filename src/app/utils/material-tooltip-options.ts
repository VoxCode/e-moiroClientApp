import {MatTooltipDefaultOptions} from '@angular/material/tooltip';

// providers: [{provide: MAT_TOOLTIP_DEFAULT_OPTIONS, useValue: myCustomTooltipDefaults}]
export const TOOLTIP_DEFAULTS: any = {
  showDelay: 300,
  hideDelay: 150,
  touchendHideDelay: 150,
};
